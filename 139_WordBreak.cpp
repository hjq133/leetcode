#include <bits/stdc++.h>
using namespace std;

bool wordBreak(string s, vector<string>& wordDict) {
    int dp[s.length()+2][s.length()+2];
    
    for(int sz=1; sz<=s.length(); sz++) {

    }
    for(int i=0; i<s.length(); i++) {  // 先枚举分割点

    }
}

int main() {
   
}